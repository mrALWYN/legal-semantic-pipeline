# ==============================
# ⚖️ Legal Semantic Pipeline - Environment Variables
# ==============================
# NOTE: DOCKER_BUILDKIT is a host variable, not loaded from .env
# Set it in your shell: export DOCKER_BUILDKIT=1 (Linux/Mac)
#                       set DOCKER_BUILDKIT=1 (Windows cmd)
#                       $env:DOCKER_BUILDKIT=1 (PowerShell)

# --- Qdrant Vector DB ---
QDRANT_HOST=qdrant
QDRANT_PORT=6333
COLLECTION_NAME=legal_precedents

# --- MLflow Tracking ---
MLFLOW_TRACKING_URI=http://mlflow:5000
REGISTERED_MODEL_NAME=legal-embed-model

# --- Paths ---
PYTHONPATH=/app
HF_HOME=/app/models

# --- GPU ---
CUDA_VISIBLE_DEVICES=0

# --- Logging ---
PYTHONUNBUFFERED=1
LOG_LEVEL=INFO